# PiMaestro — Architecture

The canonical architecture doc now lives next to the code it describes:

➡ **[`webpiano/docs/ARCHITECTURE.md`](../webpiano/docs/ARCHITECTURE.md)**

(See `VISION.md` for *why* and `BACKLOG.md` for what's done / next. This file used to be a second,
drifting copy of the architecture; it now just redirects so there is one source of truth — the
engine/server/frontend files all point at the webpiano doc.)
